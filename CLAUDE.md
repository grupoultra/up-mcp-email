# CLAUDE.md - up-mcp-email

## Descripción

MCP Server para Email (IMAP/SMTP). Provider externo de ultraPRO Desktop, ejecutado via STDIO. Implementado en Python.

## Issue Tracking

Bugs y Feature Requests se gestionan en Jira:

- **Proyecto**: ultraPRO Factory (`UPF`) en Jira, cuenta `ultra` (grupoultra.atlassian.net), project ID `10100`
- **Componente de este repo**: `email`
- **Consultar issues**: `project = UPF AND component = email`

No usar directorios `requests/` locales — Jira es la fuente de verdad.
